// src/ui/Footer.jsx
export default function Footer(){
  return (
    <footer className="app-footer">
      <small className="muted">© {new Date().getFullYear()} — Pulpo Net Computación — </small>
    </footer>
  );
}
